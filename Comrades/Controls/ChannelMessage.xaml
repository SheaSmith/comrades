<UserControl
    x:Class="Comrades.Controls.ChannelMessage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Comrades.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <PersonPicture DisplayName="{x:Bind ChatMessage.From.User.DisplayName}" Grid.Column="0" x:Name="ProfilePicture" />
        <StackPanel HorizontalAlignment="Stretch" Margin="20" Padding="16"  Grid.Column="1">
            <StackPanel MaxHeight="{x:Bind VisibleHeight, Mode=OneWay}" x:Name="textContainer" HorizontalAlignment="Stretch" SizeChanged="textContainer_SizeChanged">
                <RichTextBlock x:Name="richText" HorizontalAlignment="Stretch" SizeChanged="RichTextBlock_SizeChanged" BringIntoViewRequested="RichTextBlock_BringIntoViewRequested">
                </RichTextBlock>
            </StackPanel>

            <Button Content="{x:Bind ShowMoreText, Mode=OneWay}" Visibility="{x:Bind ShowExpand, Mode=OneWay}" Click="HyperlinkButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
